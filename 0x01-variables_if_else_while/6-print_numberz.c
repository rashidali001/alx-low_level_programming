#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int numer;


	for (numer = 0; numer <= 9; numer++)
	{
		printf("%d", numer);
	}
	putchar('\n');
	return (0);
}
